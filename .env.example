# Archivo de ejemplo de variables de entorno
# Copia este archivo a `.env` y completa los valores reales en tu entorno local.
# NO subas el archivo `.env` ni el JSON de credenciales al repositorio.

# --- AiStudio (modelo + token) - configuración mínima para correr local ---
# Copia y completa sólo estas dos variables si quieres usar AiStudio.
USE_AISTUDIO=true
AISTUDIO_API_KEY=tu_api_key_aistudio
AISTUDIO_MODEL=your-model-name
AISTUDIO_BASE_URL=https://api.aistudio.example


DEBUG=true
